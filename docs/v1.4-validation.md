# 猫眼 1.4.0 生产运维验证

验证日期：2026-08-23  
目标设备：`nasw`  
包：`community.lazycat.app.maoyan`  
交付形态：单一 LPK（独立 Collector 保持 `NotInstalled`）

## 自动化验证

- `go test ./... -count=1`
- `go vet ./...`
- `node --check web/app.js`
- LPK lint 无告警
- 加速稳定性测试写入 3,000 个指标点，同时执行并发读取、在线备份、关闭重开和完整性检查
- 备份测试覆盖 SHA-256、SQLite `quick_check`、损坏备份拒绝、恢复前安全备份和恢复后数据一致性

## nasw 升级与回滚

升级前基线：

- devices: 1
- inspections: 3
- alert_instances: 2
- metrics: 449
- SQLite quick_check: ok

验证流程：

1. 从 1.3.1 升级到 1.4.0，自动生成 `pre-upgrade` 备份。
2. 将 1.4.0 回滚安装为 1.3.1。
3. 1.3.1 正常启动，devices/inspections/alerts 数量不变，SQLite 完整性通过。
4. 重新安装 1.4.0，持久数据保留，独立 Collector 仍未安装。
5. 在运行中的应用创建手动在线备份。
6. 通过恢复 API 恢复该备份；应用使用 `exec` 原地重启，约 2 秒恢复服务。
7. 恢复请求被清理，自动生成 `pre-restore` 安全备份，SQLite 完整性通过。

## 长期稳定性

真实 7 天观测已于 **2026-08-23 20:05:39 CST** 启动，目标完成时间为
**2026-08-30 20:05:39 CST**。截至本次交付，持久化采样没有失败，但完整 7 天周期尚未结束，
因此不能将长期稳定性标记为最终通过。
