icon: ./icon.png
buildscript: |
  set -eu
  rm -rf dist/content
  mkdir -p dist/content/web
  cp -R web/. dist/content/web/
  CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -ldflags='-s -w' -o dist/content/maoyan ./server.go
contentdir: ./dist/content
