icon: ./icon.png
buildscript: |
  set -eu
  rm -rf dist/content
  mkdir -p dist/content
contentdir: ./dist/content
compose_override:
  services:
    watchcat:
      volumes:
        - /data/playground/docker.sock:/lzcapp/run/hermes-upgrade-docker.sock
